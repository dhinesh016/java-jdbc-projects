/* 
It is my dbname change the demo want you want
CREATE DATABASE demo;
*/

CREATE DATABASE db_name;
CREATE TABLE restaurant (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    location VARCHAR(50),
    fooditems VARCHAR(100),
    rating VARCHAR(10),
    description VARCHAR(200)
);
